<?php

/**
 * Base class that represents a row from the 'workshop' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Fri Mar 18 23:28:36 2011
 *
 * @package    lib.model.om
 */
abstract class BaseWorkshop extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        WorkshopPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the client_id field.
	 * @var        int
	 */
	protected $client_id;

	/**
	 * The value for the category_id field.
	 * @var        int
	 */
	protected $category_id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the workshop_date field.
	 * @var        string
	 */
	protected $workshop_date;

	/**
	 * The value for the workshop_time field.
	 * @var        string
	 */
	protected $workshop_time;

	/**
	 * The value for the location field.
	 * @var        string
	 */
	protected $location;

	/**
	 * The value for the cost field.
	 * @var        string
	 */
	protected $cost;

	/**
	 * The value for the last_day_to_register field.
	 * @var        string
	 */
	protected $last_day_to_register;

	/**
	 * The value for the max_nbr_attendees field.
	 * @var        int
	 */
	protected $max_nbr_attendees;

	/**
	 * The value for the nbr_current_attendees field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $nbr_current_attendees;

	/**
	 * The value for the description field.
	 * @var        string
	 */
	protected $description;

	/**
	 * The value for the logistics field.
	 * @var        string
	 */
	protected $logistics;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        Client
	 */
	protected $aClient;

	/**
	 * @var        Category
	 */
	protected $aCategory;

	/**
	 * @var        array Registrant[] Collection to store aggregation of Registrant objects.
	 */
	protected $collRegistrants;

	/**
	 * @var        Criteria The criteria used to select the current contents of collRegistrants.
	 */
	private $lastRegistrantCriteria = null;

	/**
	 * @var        array RegistrationEmail[] Collection to store aggregation of RegistrationEmail objects.
	 */
	protected $collRegistrationEmails;

	/**
	 * @var        Criteria The criteria used to select the current contents of collRegistrationEmails.
	 */
	private $lastRegistrationEmailCriteria = null;

	/**
	 * @var        array WorkshopExtra[] Collection to store aggregation of WorkshopExtra objects.
	 */
	protected $collWorkshopExtras;

	/**
	 * @var        Criteria The criteria used to select the current contents of collWorkshopExtras.
	 */
	private $lastWorkshopExtraCriteria = null;

	/**
	 * @var        array WorkshopRegistrant[] Collection to store aggregation of WorkshopRegistrant objects.
	 */
	protected $collWorkshopRegistrants;

	/**
	 * @var        Criteria The criteria used to select the current contents of collWorkshopRegistrants.
	 */
	private $lastWorkshopRegistrantCriteria = null;

	/**
	 * @var        array SponsorLogo[] Collection to store aggregation of SponsorLogo objects.
	 */
	protected $collSponsorLogos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSponsorLogos.
	 */
	private $lastSponsorLogoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'WorkshopPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->nbr_current_attendees = 0;
	}

	/**
	 * Initializes internal state of BaseWorkshop object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [client_id] column value.
	 * 
	 * @return     int
	 */
	public function getClientId()
	{
		return $this->client_id;
	}

	/**
	 * Get the [category_id] column value.
	 * 
	 * @return     int
	 */
	public function getCategoryId()
	{
		return $this->category_id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [optionally formatted] temporal [workshop_date] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getWorkshopDate($format = 'Y-m-d')
	{
		if ($this->workshop_date === null) {
			return null;
		}


		if ($this->workshop_date === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->workshop_date);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->workshop_date, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [workshop_time] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getWorkshopTime($format = 'H:i:s')
	{
		if ($this->workshop_time === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->workshop_time);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->workshop_time, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [location] column value.
	 * 
	 * @return     string
	 */
	public function getLocation()
	{
		return $this->location;
	}

	/**
	 * Get the [cost] column value.
	 * 
	 * @return     string
	 */
	public function getCost()
	{
		return $this->cost;
	}

	/**
	 * Get the [optionally formatted] temporal [last_day_to_register] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getLastDayToRegister($format = 'Y-m-d')
	{
		if ($this->last_day_to_register === null) {
			return null;
		}


		if ($this->last_day_to_register === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->last_day_to_register);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->last_day_to_register, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [max_nbr_attendees] column value.
	 * 
	 * @return     int
	 */
	public function getMaxNbrAttendees()
	{
		return $this->max_nbr_attendees;
	}

	/**
	 * Get the [nbr_current_attendees] column value.
	 * 
	 * @return     int
	 */
	public function getNbrCurrentAttendees()
	{
		return $this->nbr_current_attendees;
	}

	/**
	 * Get the [description] column value.
	 * 
	 * @return     string
	 */
	public function getDescription()
	{
		return $this->description;
	}

	/**
	 * Get the [logistics] column value.
	 * 
	 * @return     string
	 */
	public function getLogistics()
	{
		return $this->logistics;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = WorkshopPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [client_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setClientId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->client_id !== $v) {
			$this->client_id = $v;
			$this->modifiedColumns[] = WorkshopPeer::CLIENT_ID;
		}

		if ($this->aClient !== null && $this->aClient->getId() !== $v) {
			$this->aClient = null;
		}

		return $this;
	} // setClientId()

	/**
	 * Set the value of [category_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setCategoryId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->category_id !== $v) {
			$this->category_id = $v;
			$this->modifiedColumns[] = WorkshopPeer::CATEGORY_ID;
		}

		if ($this->aCategory !== null && $this->aCategory->getId() !== $v) {
			$this->aCategory = null;
		}

		return $this;
	} // setCategoryId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = WorkshopPeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Sets the value of [workshop_date] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setWorkshopDate($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->workshop_date !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->workshop_date !== null && $tmpDt = new DateTime($this->workshop_date)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->workshop_date = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = WorkshopPeer::WORKSHOP_DATE;
			}
		} // if either are not null

		return $this;
	} // setWorkshopDate()

	/**
	 * Sets the value of [workshop_time] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setWorkshopTime($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->workshop_time !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->workshop_time !== null && $tmpDt = new DateTime($this->workshop_time)) ? $tmpDt->format('H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->workshop_time = ($dt ? $dt->format('H:i:s') : null);
				$this->modifiedColumns[] = WorkshopPeer::WORKSHOP_TIME;
			}
		} // if either are not null

		return $this;
	} // setWorkshopTime()

	/**
	 * Set the value of [location] column.
	 * 
	 * @param      string $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setLocation($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->location !== $v) {
			$this->location = $v;
			$this->modifiedColumns[] = WorkshopPeer::LOCATION;
		}

		return $this;
	} // setLocation()

	/**
	 * Set the value of [cost] column.
	 * 
	 * @param      string $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setCost($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cost !== $v) {
			$this->cost = $v;
			$this->modifiedColumns[] = WorkshopPeer::COST;
		}

		return $this;
	} // setCost()

	/**
	 * Sets the value of [last_day_to_register] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setLastDayToRegister($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->last_day_to_register !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->last_day_to_register !== null && $tmpDt = new DateTime($this->last_day_to_register)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->last_day_to_register = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = WorkshopPeer::LAST_DAY_TO_REGISTER;
			}
		} // if either are not null

		return $this;
	} // setLastDayToRegister()

	/**
	 * Set the value of [max_nbr_attendees] column.
	 * 
	 * @param      int $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setMaxNbrAttendees($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->max_nbr_attendees !== $v) {
			$this->max_nbr_attendees = $v;
			$this->modifiedColumns[] = WorkshopPeer::MAX_NBR_ATTENDEES;
		}

		return $this;
	} // setMaxNbrAttendees()

	/**
	 * Set the value of [nbr_current_attendees] column.
	 * 
	 * @param      int $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setNbrCurrentAttendees($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->nbr_current_attendees !== $v || $this->isNew()) {
			$this->nbr_current_attendees = $v;
			$this->modifiedColumns[] = WorkshopPeer::NBR_CURRENT_ATTENDEES;
		}

		return $this;
	} // setNbrCurrentAttendees()

	/**
	 * Set the value of [description] column.
	 * 
	 * @param      string $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setDescription($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->description !== $v) {
			$this->description = $v;
			$this->modifiedColumns[] = WorkshopPeer::DESCRIPTION;
		}

		return $this;
	} // setDescription()

	/**
	 * Set the value of [logistics] column.
	 * 
	 * @param      string $v new value
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setLogistics($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->logistics !== $v) {
			$this->logistics = $v;
			$this->modifiedColumns[] = WorkshopPeer::LOGISTICS;
		}

		return $this;
	} // setLogistics()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = WorkshopPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Workshop The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = WorkshopPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->nbr_current_attendees !== 0) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->client_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->category_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->name = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->workshop_date = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->workshop_time = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->location = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->cost = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->last_day_to_register = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->max_nbr_attendees = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->nbr_current_attendees = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->description = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->logistics = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->created_at = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->updated_at = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 15; // 15 = WorkshopPeer::NUM_COLUMNS - WorkshopPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Workshop object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aClient !== null && $this->client_id !== $this->aClient->getId()) {
			$this->aClient = null;
		}
		if ($this->aCategory !== null && $this->category_id !== $this->aCategory->getId()) {
			$this->aCategory = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(WorkshopPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = WorkshopPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aClient = null;
			$this->aCategory = null;
			$this->collRegistrants = null;
			$this->lastRegistrantCriteria = null;

			$this->collRegistrationEmails = null;
			$this->lastRegistrationEmailCriteria = null;

			$this->collWorkshopExtras = null;
			$this->lastWorkshopExtraCriteria = null;

			$this->collWorkshopRegistrants = null;
			$this->lastWorkshopRegistrantCriteria = null;

			$this->collSponsorLogos = null;
			$this->lastSponsorLogoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(WorkshopPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseWorkshop:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				WorkshopPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseWorkshop:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(WorkshopPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseWorkshop:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			if ($this->isModified() && !$this->isColumnModified(WorkshopPeer::UPDATED_AT))
			{
			  $this->setUpdatedAt(time());
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(WorkshopPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseWorkshop:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				WorkshopPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aClient !== null) {
				if ($this->aClient->isModified() || $this->aClient->isNew()) {
					$affectedRows += $this->aClient->save($con);
				}
				$this->setClient($this->aClient);
			}

			if ($this->aCategory !== null) {
				if ($this->aCategory->isModified() || $this->aCategory->isNew()) {
					$affectedRows += $this->aCategory->save($con);
				}
				$this->setCategory($this->aCategory);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = WorkshopPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = WorkshopPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += WorkshopPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collRegistrants !== null) {
				foreach ($this->collRegistrants as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collRegistrationEmails !== null) {
				foreach ($this->collRegistrationEmails as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collWorkshopExtras !== null) {
				foreach ($this->collWorkshopExtras as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collWorkshopRegistrants !== null) {
				foreach ($this->collWorkshopRegistrants as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSponsorLogos !== null) {
				foreach ($this->collSponsorLogos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aClient !== null) {
				if (!$this->aClient->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aClient->getValidationFailures());
				}
			}

			if ($this->aCategory !== null) {
				if (!$this->aCategory->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCategory->getValidationFailures());
				}
			}


			if (($retval = WorkshopPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collRegistrants !== null) {
					foreach ($this->collRegistrants as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collRegistrationEmails !== null) {
					foreach ($this->collRegistrationEmails as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collWorkshopExtras !== null) {
					foreach ($this->collWorkshopExtras as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collWorkshopRegistrants !== null) {
					foreach ($this->collWorkshopRegistrants as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSponsorLogos !== null) {
					foreach ($this->collSponsorLogos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = WorkshopPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getClientId();
				break;
			case 2:
				return $this->getCategoryId();
				break;
			case 3:
				return $this->getName();
				break;
			case 4:
				return $this->getWorkshopDate();
				break;
			case 5:
				return $this->getWorkshopTime();
				break;
			case 6:
				return $this->getLocation();
				break;
			case 7:
				return $this->getCost();
				break;
			case 8:
				return $this->getLastDayToRegister();
				break;
			case 9:
				return $this->getMaxNbrAttendees();
				break;
			case 10:
				return $this->getNbrCurrentAttendees();
				break;
			case 11:
				return $this->getDescription();
				break;
			case 12:
				return $this->getLogistics();
				break;
			case 13:
				return $this->getCreatedAt();
				break;
			case 14:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = WorkshopPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getClientId(),
			$keys[2] => $this->getCategoryId(),
			$keys[3] => $this->getName(),
			$keys[4] => $this->getWorkshopDate(),
			$keys[5] => $this->getWorkshopTime(),
			$keys[6] => $this->getLocation(),
			$keys[7] => $this->getCost(),
			$keys[8] => $this->getLastDayToRegister(),
			$keys[9] => $this->getMaxNbrAttendees(),
			$keys[10] => $this->getNbrCurrentAttendees(),
			$keys[11] => $this->getDescription(),
			$keys[12] => $this->getLogistics(),
			$keys[13] => $this->getCreatedAt(),
			$keys[14] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = WorkshopPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setClientId($value);
				break;
			case 2:
				$this->setCategoryId($value);
				break;
			case 3:
				$this->setName($value);
				break;
			case 4:
				$this->setWorkshopDate($value);
				break;
			case 5:
				$this->setWorkshopTime($value);
				break;
			case 6:
				$this->setLocation($value);
				break;
			case 7:
				$this->setCost($value);
				break;
			case 8:
				$this->setLastDayToRegister($value);
				break;
			case 9:
				$this->setMaxNbrAttendees($value);
				break;
			case 10:
				$this->setNbrCurrentAttendees($value);
				break;
			case 11:
				$this->setDescription($value);
				break;
			case 12:
				$this->setLogistics($value);
				break;
			case 13:
				$this->setCreatedAt($value);
				break;
			case 14:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = WorkshopPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setClientId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCategoryId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setName($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setWorkshopDate($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setWorkshopTime($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setLocation($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCost($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setLastDayToRegister($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setMaxNbrAttendees($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setNbrCurrentAttendees($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setDescription($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setLogistics($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setCreatedAt($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setUpdatedAt($arr[$keys[14]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);

		if ($this->isColumnModified(WorkshopPeer::ID)) $criteria->add(WorkshopPeer::ID, $this->id);
		if ($this->isColumnModified(WorkshopPeer::CLIENT_ID)) $criteria->add(WorkshopPeer::CLIENT_ID, $this->client_id);
		if ($this->isColumnModified(WorkshopPeer::CATEGORY_ID)) $criteria->add(WorkshopPeer::CATEGORY_ID, $this->category_id);
		if ($this->isColumnModified(WorkshopPeer::NAME)) $criteria->add(WorkshopPeer::NAME, $this->name);
		if ($this->isColumnModified(WorkshopPeer::WORKSHOP_DATE)) $criteria->add(WorkshopPeer::WORKSHOP_DATE, $this->workshop_date);
		if ($this->isColumnModified(WorkshopPeer::WORKSHOP_TIME)) $criteria->add(WorkshopPeer::WORKSHOP_TIME, $this->workshop_time);
		if ($this->isColumnModified(WorkshopPeer::LOCATION)) $criteria->add(WorkshopPeer::LOCATION, $this->location);
		if ($this->isColumnModified(WorkshopPeer::COST)) $criteria->add(WorkshopPeer::COST, $this->cost);
		if ($this->isColumnModified(WorkshopPeer::LAST_DAY_TO_REGISTER)) $criteria->add(WorkshopPeer::LAST_DAY_TO_REGISTER, $this->last_day_to_register);
		if ($this->isColumnModified(WorkshopPeer::MAX_NBR_ATTENDEES)) $criteria->add(WorkshopPeer::MAX_NBR_ATTENDEES, $this->max_nbr_attendees);
		if ($this->isColumnModified(WorkshopPeer::NBR_CURRENT_ATTENDEES)) $criteria->add(WorkshopPeer::NBR_CURRENT_ATTENDEES, $this->nbr_current_attendees);
		if ($this->isColumnModified(WorkshopPeer::DESCRIPTION)) $criteria->add(WorkshopPeer::DESCRIPTION, $this->description);
		if ($this->isColumnModified(WorkshopPeer::LOGISTICS)) $criteria->add(WorkshopPeer::LOGISTICS, $this->logistics);
		if ($this->isColumnModified(WorkshopPeer::CREATED_AT)) $criteria->add(WorkshopPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(WorkshopPeer::UPDATED_AT)) $criteria->add(WorkshopPeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);

		$criteria->add(WorkshopPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Workshop (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setClientId($this->client_id);

		$copyObj->setCategoryId($this->category_id);

		$copyObj->setName($this->name);

		$copyObj->setWorkshopDate($this->workshop_date);

		$copyObj->setWorkshopTime($this->workshop_time);

		$copyObj->setLocation($this->location);

		$copyObj->setCost($this->cost);

		$copyObj->setLastDayToRegister($this->last_day_to_register);

		$copyObj->setMaxNbrAttendees($this->max_nbr_attendees);

		$copyObj->setNbrCurrentAttendees($this->nbr_current_attendees);

		$copyObj->setDescription($this->description);

		$copyObj->setLogistics($this->logistics);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getRegistrants() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addRegistrant($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getRegistrationEmails() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addRegistrationEmail($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getWorkshopExtras() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addWorkshopExtra($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getWorkshopRegistrants() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addWorkshopRegistrant($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSponsorLogos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSponsorLogo($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Workshop Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     WorkshopPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new WorkshopPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Client object.
	 *
	 * @param      Client $v
	 * @return     Workshop The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setClient(Client $v = null)
	{
		if ($v === null) {
			$this->setClientId(NULL);
		} else {
			$this->setClientId($v->getId());
		}

		$this->aClient = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Client object, it will not be re-added.
		if ($v !== null) {
			$v->addWorkshop($this);
		}

		return $this;
	}


	/**
	 * Get the associated Client object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Client The associated Client object.
	 * @throws     PropelException
	 */
	public function getClient(PropelPDO $con = null)
	{
		if ($this->aClient === null && ($this->client_id !== null)) {
			$this->aClient = ClientPeer::retrieveByPk($this->client_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aClient->addWorkshops($this);
			 */
		}
		return $this->aClient;
	}

	/**
	 * Declares an association between this object and a Category object.
	 *
	 * @param      Category $v
	 * @return     Workshop The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCategory(Category $v = null)
	{
		if ($v === null) {
			$this->setCategoryId(NULL);
		} else {
			$this->setCategoryId($v->getId());
		}

		$this->aCategory = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Category object, it will not be re-added.
		if ($v !== null) {
			$v->addWorkshop($this);
		}

		return $this;
	}


	/**
	 * Get the associated Category object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Category The associated Category object.
	 * @throws     PropelException
	 */
	public function getCategory(PropelPDO $con = null)
	{
		if ($this->aCategory === null && ($this->category_id !== null)) {
			$this->aCategory = CategoryPeer::retrieveByPk($this->category_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCategory->addWorkshops($this);
			 */
		}
		return $this->aCategory;
	}

	/**
	 * Clears out the collRegistrants collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addRegistrants()
	 */
	public function clearRegistrants()
	{
		$this->collRegistrants = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collRegistrants collection (array).
	 *
	 * By default this just sets the collRegistrants collection to an empty array (like clearcollRegistrants());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initRegistrants()
	{
		$this->collRegistrants = array();
	}

	/**
	 * Gets an array of Registrant objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Workshop has previously been saved, it will retrieve
	 * related Registrants from storage. If this Workshop is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Registrant[]
	 * @throws     PropelException
	 */
	public function getRegistrants($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRegistrants === null) {
			if ($this->isNew()) {
			   $this->collRegistrants = array();
			} else {

				$criteria->add(RegistrantPeer::WORKSHOP_ID, $this->id);

				RegistrantPeer::addSelectColumns($criteria);
				$this->collRegistrants = RegistrantPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(RegistrantPeer::WORKSHOP_ID, $this->id);

				RegistrantPeer::addSelectColumns($criteria);
				if (!isset($this->lastRegistrantCriteria) || !$this->lastRegistrantCriteria->equals($criteria)) {
					$this->collRegistrants = RegistrantPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastRegistrantCriteria = $criteria;
		return $this->collRegistrants;
	}

	/**
	 * Returns the number of related Registrant objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Registrant objects.
	 * @throws     PropelException
	 */
	public function countRegistrants(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collRegistrants === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(RegistrantPeer::WORKSHOP_ID, $this->id);

				$count = RegistrantPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(RegistrantPeer::WORKSHOP_ID, $this->id);

				if (!isset($this->lastRegistrantCriteria) || !$this->lastRegistrantCriteria->equals($criteria)) {
					$count = RegistrantPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collRegistrants);
				}
			} else {
				$count = count($this->collRegistrants);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Registrant object to this object
	 * through the Registrant foreign key attribute.
	 *
	 * @param      Registrant $l Registrant
	 * @return     void
	 * @throws     PropelException
	 */
	public function addRegistrant(Registrant $l)
	{
		if ($this->collRegistrants === null) {
			$this->initRegistrants();
		}
		if (!in_array($l, $this->collRegistrants, true)) { // only add it if the **same** object is not already associated
			array_push($this->collRegistrants, $l);
			$l->setWorkshop($this);
		}
	}

	/**
	 * Clears out the collRegistrationEmails collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addRegistrationEmails()
	 */
	public function clearRegistrationEmails()
	{
		$this->collRegistrationEmails = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collRegistrationEmails collection (array).
	 *
	 * By default this just sets the collRegistrationEmails collection to an empty array (like clearcollRegistrationEmails());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initRegistrationEmails()
	{
		$this->collRegistrationEmails = array();
	}

	/**
	 * Gets an array of RegistrationEmail objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Workshop has previously been saved, it will retrieve
	 * related RegistrationEmails from storage. If this Workshop is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array RegistrationEmail[]
	 * @throws     PropelException
	 */
	public function getRegistrationEmails($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRegistrationEmails === null) {
			if ($this->isNew()) {
			   $this->collRegistrationEmails = array();
			} else {

				$criteria->add(RegistrationEmailPeer::WORKSHOP_ID, $this->id);

				RegistrationEmailPeer::addSelectColumns($criteria);
				$this->collRegistrationEmails = RegistrationEmailPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(RegistrationEmailPeer::WORKSHOP_ID, $this->id);

				RegistrationEmailPeer::addSelectColumns($criteria);
				if (!isset($this->lastRegistrationEmailCriteria) || !$this->lastRegistrationEmailCriteria->equals($criteria)) {
					$this->collRegistrationEmails = RegistrationEmailPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastRegistrationEmailCriteria = $criteria;
		return $this->collRegistrationEmails;
	}

	/**
	 * Returns the number of related RegistrationEmail objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related RegistrationEmail objects.
	 * @throws     PropelException
	 */
	public function countRegistrationEmails(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collRegistrationEmails === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(RegistrationEmailPeer::WORKSHOP_ID, $this->id);

				$count = RegistrationEmailPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(RegistrationEmailPeer::WORKSHOP_ID, $this->id);

				if (!isset($this->lastRegistrationEmailCriteria) || !$this->lastRegistrationEmailCriteria->equals($criteria)) {
					$count = RegistrationEmailPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collRegistrationEmails);
				}
			} else {
				$count = count($this->collRegistrationEmails);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a RegistrationEmail object to this object
	 * through the RegistrationEmail foreign key attribute.
	 *
	 * @param      RegistrationEmail $l RegistrationEmail
	 * @return     void
	 * @throws     PropelException
	 */
	public function addRegistrationEmail(RegistrationEmail $l)
	{
		if ($this->collRegistrationEmails === null) {
			$this->initRegistrationEmails();
		}
		if (!in_array($l, $this->collRegistrationEmails, true)) { // only add it if the **same** object is not already associated
			array_push($this->collRegistrationEmails, $l);
			$l->setWorkshop($this);
		}
	}

	/**
	 * Clears out the collWorkshopExtras collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addWorkshopExtras()
	 */
	public function clearWorkshopExtras()
	{
		$this->collWorkshopExtras = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collWorkshopExtras collection (array).
	 *
	 * By default this just sets the collWorkshopExtras collection to an empty array (like clearcollWorkshopExtras());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initWorkshopExtras()
	{
		$this->collWorkshopExtras = array();
	}

	/**
	 * Gets an array of WorkshopExtra objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Workshop has previously been saved, it will retrieve
	 * related WorkshopExtras from storage. If this Workshop is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array WorkshopExtra[]
	 * @throws     PropelException
	 */
	public function getWorkshopExtras($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collWorkshopExtras === null) {
			if ($this->isNew()) {
			   $this->collWorkshopExtras = array();
			} else {

				$criteria->add(WorkshopExtraPeer::WORKSHOP_ID, $this->id);

				WorkshopExtraPeer::addSelectColumns($criteria);
				$this->collWorkshopExtras = WorkshopExtraPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(WorkshopExtraPeer::WORKSHOP_ID, $this->id);

				WorkshopExtraPeer::addSelectColumns($criteria);
				if (!isset($this->lastWorkshopExtraCriteria) || !$this->lastWorkshopExtraCriteria->equals($criteria)) {
					$this->collWorkshopExtras = WorkshopExtraPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastWorkshopExtraCriteria = $criteria;
		return $this->collWorkshopExtras;
	}

	/**
	 * Returns the number of related WorkshopExtra objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related WorkshopExtra objects.
	 * @throws     PropelException
	 */
	public function countWorkshopExtras(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collWorkshopExtras === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(WorkshopExtraPeer::WORKSHOP_ID, $this->id);

				$count = WorkshopExtraPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(WorkshopExtraPeer::WORKSHOP_ID, $this->id);

				if (!isset($this->lastWorkshopExtraCriteria) || !$this->lastWorkshopExtraCriteria->equals($criteria)) {
					$count = WorkshopExtraPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collWorkshopExtras);
				}
			} else {
				$count = count($this->collWorkshopExtras);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a WorkshopExtra object to this object
	 * through the WorkshopExtra foreign key attribute.
	 *
	 * @param      WorkshopExtra $l WorkshopExtra
	 * @return     void
	 * @throws     PropelException
	 */
	public function addWorkshopExtra(WorkshopExtra $l)
	{
		if ($this->collWorkshopExtras === null) {
			$this->initWorkshopExtras();
		}
		if (!in_array($l, $this->collWorkshopExtras, true)) { // only add it if the **same** object is not already associated
			array_push($this->collWorkshopExtras, $l);
			$l->setWorkshop($this);
		}
	}

	/**
	 * Clears out the collWorkshopRegistrants collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addWorkshopRegistrants()
	 */
	public function clearWorkshopRegistrants()
	{
		$this->collWorkshopRegistrants = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collWorkshopRegistrants collection (array).
	 *
	 * By default this just sets the collWorkshopRegistrants collection to an empty array (like clearcollWorkshopRegistrants());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initWorkshopRegistrants()
	{
		$this->collWorkshopRegistrants = array();
	}

	/**
	 * Gets an array of WorkshopRegistrant objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Workshop has previously been saved, it will retrieve
	 * related WorkshopRegistrants from storage. If this Workshop is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array WorkshopRegistrant[]
	 * @throws     PropelException
	 */
	public function getWorkshopRegistrants($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collWorkshopRegistrants === null) {
			if ($this->isNew()) {
			   $this->collWorkshopRegistrants = array();
			} else {

				$criteria->add(WorkshopRegistrantPeer::WORKSHOP_ID, $this->id);

				WorkshopRegistrantPeer::addSelectColumns($criteria);
				$this->collWorkshopRegistrants = WorkshopRegistrantPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(WorkshopRegistrantPeer::WORKSHOP_ID, $this->id);

				WorkshopRegistrantPeer::addSelectColumns($criteria);
				if (!isset($this->lastWorkshopRegistrantCriteria) || !$this->lastWorkshopRegistrantCriteria->equals($criteria)) {
					$this->collWorkshopRegistrants = WorkshopRegistrantPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastWorkshopRegistrantCriteria = $criteria;
		return $this->collWorkshopRegistrants;
	}

	/**
	 * Returns the number of related WorkshopRegistrant objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related WorkshopRegistrant objects.
	 * @throws     PropelException
	 */
	public function countWorkshopRegistrants(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collWorkshopRegistrants === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(WorkshopRegistrantPeer::WORKSHOP_ID, $this->id);

				$count = WorkshopRegistrantPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(WorkshopRegistrantPeer::WORKSHOP_ID, $this->id);

				if (!isset($this->lastWorkshopRegistrantCriteria) || !$this->lastWorkshopRegistrantCriteria->equals($criteria)) {
					$count = WorkshopRegistrantPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collWorkshopRegistrants);
				}
			} else {
				$count = count($this->collWorkshopRegistrants);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a WorkshopRegistrant object to this object
	 * through the WorkshopRegistrant foreign key attribute.
	 *
	 * @param      WorkshopRegistrant $l WorkshopRegistrant
	 * @return     void
	 * @throws     PropelException
	 */
	public function addWorkshopRegistrant(WorkshopRegistrant $l)
	{
		if ($this->collWorkshopRegistrants === null) {
			$this->initWorkshopRegistrants();
		}
		if (!in_array($l, $this->collWorkshopRegistrants, true)) { // only add it if the **same** object is not already associated
			array_push($this->collWorkshopRegistrants, $l);
			$l->setWorkshop($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Workshop is new, it will return
	 * an empty collection; or if this Workshop has previously
	 * been saved, it will retrieve related WorkshopRegistrants from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Workshop.
	 */
	public function getWorkshopRegistrantsJoinRegistrant($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collWorkshopRegistrants === null) {
			if ($this->isNew()) {
				$this->collWorkshopRegistrants = array();
			} else {

				$criteria->add(WorkshopRegistrantPeer::WORKSHOP_ID, $this->id);

				$this->collWorkshopRegistrants = WorkshopRegistrantPeer::doSelectJoinRegistrant($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(WorkshopRegistrantPeer::WORKSHOP_ID, $this->id);

			if (!isset($this->lastWorkshopRegistrantCriteria) || !$this->lastWorkshopRegistrantCriteria->equals($criteria)) {
				$this->collWorkshopRegistrants = WorkshopRegistrantPeer::doSelectJoinRegistrant($criteria, $con, $join_behavior);
			}
		}
		$this->lastWorkshopRegistrantCriteria = $criteria;

		return $this->collWorkshopRegistrants;
	}

	/**
	 * Clears out the collSponsorLogos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSponsorLogos()
	 */
	public function clearSponsorLogos()
	{
		$this->collSponsorLogos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSponsorLogos collection (array).
	 *
	 * By default this just sets the collSponsorLogos collection to an empty array (like clearcollSponsorLogos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSponsorLogos()
	{
		$this->collSponsorLogos = array();
	}

	/**
	 * Gets an array of SponsorLogo objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Workshop has previously been saved, it will retrieve
	 * related SponsorLogos from storage. If this Workshop is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SponsorLogo[]
	 * @throws     PropelException
	 */
	public function getSponsorLogos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSponsorLogos === null) {
			if ($this->isNew()) {
			   $this->collSponsorLogos = array();
			} else {

				$criteria->add(SponsorLogoPeer::WORKSHOP_ID, $this->id);

				SponsorLogoPeer::addSelectColumns($criteria);
				$this->collSponsorLogos = SponsorLogoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SponsorLogoPeer::WORKSHOP_ID, $this->id);

				SponsorLogoPeer::addSelectColumns($criteria);
				if (!isset($this->lastSponsorLogoCriteria) || !$this->lastSponsorLogoCriteria->equals($criteria)) {
					$this->collSponsorLogos = SponsorLogoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSponsorLogoCriteria = $criteria;
		return $this->collSponsorLogos;
	}

	/**
	 * Returns the number of related SponsorLogo objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SponsorLogo objects.
	 * @throws     PropelException
	 */
	public function countSponsorLogos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(WorkshopPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSponsorLogos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SponsorLogoPeer::WORKSHOP_ID, $this->id);

				$count = SponsorLogoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SponsorLogoPeer::WORKSHOP_ID, $this->id);

				if (!isset($this->lastSponsorLogoCriteria) || !$this->lastSponsorLogoCriteria->equals($criteria)) {
					$count = SponsorLogoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSponsorLogos);
				}
			} else {
				$count = count($this->collSponsorLogos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SponsorLogo object to this object
	 * through the SponsorLogo foreign key attribute.
	 *
	 * @param      SponsorLogo $l SponsorLogo
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSponsorLogo(SponsorLogo $l)
	{
		if ($this->collSponsorLogos === null) {
			$this->initSponsorLogos();
		}
		if (!in_array($l, $this->collSponsorLogos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSponsorLogos, $l);
			$l->setWorkshop($this);
		}
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collRegistrants) {
				foreach ((array) $this->collRegistrants as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collRegistrationEmails) {
				foreach ((array) $this->collRegistrationEmails as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collWorkshopExtras) {
				foreach ((array) $this->collWorkshopExtras as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collWorkshopRegistrants) {
				foreach ((array) $this->collWorkshopRegistrants as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSponsorLogos) {
				foreach ((array) $this->collSponsorLogos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collRegistrants = null;
		$this->collRegistrationEmails = null;
		$this->collWorkshopExtras = null;
		$this->collWorkshopRegistrants = null;
		$this->collSponsorLogos = null;
			$this->aClient = null;
			$this->aCategory = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseWorkshop:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseWorkshop::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseWorkshop
